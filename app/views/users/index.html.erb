<h1>List of Active Users!</h1>

<ul>
  <% @users.each do |user| %>
    <li><%= link_to user.email, user_path(user) %></li>
        Number of Movies in Collection: <%= user.movies.count %><br><br>
  <% end %>
</ul>
